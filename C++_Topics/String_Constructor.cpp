#include<bits/stdc++.h>
using namespace std;

int main(){
    
    // 1
    // string s="hello world";
    
    // 2
    // string s("hello world");
    
    
    // 3
    // string s("hello world", 4);
    
    /* 
    
    if a string is given directly as input to constructor and a number 4 is also given as input seperated by a , then it gives first 4 characters
    
    if a string is at first taken to another string variable and then it is given to a string as input and a number 4 is also given as input seperated by a , then it gives the characters except the first 4 characters

    */
    
    // 4
    // string a = "hello world";
    // string s(a, 4);

    // 5
    string s(26, 'a');
    cout << s << endl; 

    return 0;
}