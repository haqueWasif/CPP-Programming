#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin >> s;

    sort(s.begin(), s.end());
    // starting pointer and pointer to next to the last character

    cout << s << endl;
    
    return 0;
}